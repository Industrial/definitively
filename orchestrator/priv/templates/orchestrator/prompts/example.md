You are running an example orchestrator workflow step.

Replace this prompt with task-specific instructions for LLM nodes.
Add more `.md` files here and reference them from program YAML (`prompt:` on approval or LLM states).
